{
  "name": "video-meetings",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"npm run dev --workspace @video-meetings/web\" \"npm run dev --workspace @video-meetings/api\"",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently -n web,api -c cyan,green \"npm run start --workspace @video-meetings/web\" \"npm run start --workspace @video-meetings/api\"",
    "db:up": "docker compose up -d postgres",
    "db:ready": "docker compose up -d --wait --wait-timeout 120 postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:migrate": "npm run prisma --workspace @video-meetings/api -- migrate deploy --schema ./prisma/schema.prisma",
    "lint": "npm run lint:scripts && npm run lint --workspaces --if-present",
    "lint:scripts": "eslint scripts",
    "test:e2e:api": "npm run test:e2e --workspace @video-meetings/api",
    "test:e2e:web": "npm run test:e2e --workspace @video-meetings/web",
    "test:ralph": "node --test --test-concurrency=1 scripts/ralph/ralph-claude.test.mjs scripts/ralph/ralph-codex.test.mjs scripts/ralph/ralph-control-plane.test.mjs scripts/ralph/ralph-issues.test.mjs scripts/ralph/ralph-metrics.test.mjs scripts/ralph/ralph-orchestration.test.mjs scripts/ralph/ralph-prompts.test.mjs scripts/ralph/ralph-runtime.test.mjs scripts/ralph/ralph-state.test.mjs scripts/ralph/ralph-validation.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "test:e2e:web:visual": "npm run test:e2e:visual --workspace @video-meetings/web"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.4",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.65.0"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  }
}
